{
  "name": "wakeup-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Wake Up ☕️ — אתר מכירה + checkout. שלד ראשוני (גל 1, סוכן 1).",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test:payments": "node scripts/test-payment-adapter.mjs",
    "loadtest:local": "node scripts/local-load-test.mjs"
  },
  "dependencies": {
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.7.3"
  }
}
